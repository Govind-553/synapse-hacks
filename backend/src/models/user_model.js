/**
 * @fileoverview User model representing the 'Users' table in the MySQL database.
 */

// This is a simple file to define and export the table name.
// The business logic and queries will reside in the service layer.
const USER_TABLE = 'Users';

module.exports = {
  USER_TABLE,
};
